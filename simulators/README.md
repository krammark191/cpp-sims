# Simulator Imports

The `legacy/` folder under each simulator contains the original imported course project sources copied into this merged workspace.

Current status:

- `lander/legacy`: imported
- `howitzer/legacy`: imported
- `chess/legacy`: imported
- `orbital/legacy`: imported

These sources are preserved for staged integration. They are not yet compiled into the merged app target.
